version: "2.1"
services:
  javsp:
    image: javsp_image:latest
    container_name: javsp
    environment:
      - PUID=${PUID}    # 替换或者删除
      - PGID=${PGID}    # 替换或者删除
      - TZ=${TZ}        # 替换或者删除
    ports:
      - 8501:8501                              
    restart: always
